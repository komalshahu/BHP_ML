services:
  - type: web
    name: bangalore-home-price-estimator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT server.server:app
    envVars:
      - key: PORT
        value: 10000
